name: Discover Equestrian MFE CI

on:
  push:
    paths:
      - 'projects/discover-equestrian/**'
  pull_request:
    paths:
      - 'projects/discover-equestrian/**'

jobs:
  test_and_build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Setup Node
        uses: actions/setup-node@v3
        with:
          node-version: 18
      - name: Install
        run: cd projects/discover-equestrian && npm install
      - name: Lint
        run: cd projects/discover-equestrian && npm run lint
      - name: Test
        run: cd projects/discover-equestrian && npm test || echo "No tests"
      - name: Build
        run: cd projects/discover-equestrian && npm run build
