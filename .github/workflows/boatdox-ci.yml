name: BoatDox MFE CI

on:
  push:
    paths:
      - 'projects/boatdox/**'
  pull_request:
    paths:
      - 'projects/boatdox/**'

jobs:
  test_and_build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Setup Node
        uses: actions/setup-node@v3
        with:
          node-version: 18
      - name: Install
        run: cd projects/boatdox && npm install
      - name: Lint
        run: cd projects/boatdox && npm run lint
      - name: Test
        run: cd projects/boatdox && npm test || echo "No tests"
      - name: Build
        run: cd projects/boatdox && npm run build
